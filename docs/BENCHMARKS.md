# Benchmarks

No physical-device benchmark numbers have been recorded yet. The repository does not invent results.

The next benchmark pass must use identical 10-, 30-, and 60-second permitted speech clips on the OPPO device and record model load, first result, full inference, translation time, peak RSS, CPU, temperature, and battery change for 2/4/6 threads. Compare Tiny and Base, then assess VAD and KleidiAI only after the raw capture path is proven.
